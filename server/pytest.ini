[pytest]
pythonpath = .
testpaths = tests
markers =
    slow: carga el modelo entrenado (~1.2GB) desde disco; se salta si datos/modelos_patrones_v9.joblib no existe
filterwarnings =
    ignore:Setting the shape on a NumPy array has been deprecated:DeprecationWarning
